# Solicita al usuario que ingrese un número entero y lo almacena en la variable 'numero'
numero = int(input("Ingrese un número: "))

# Verifica si el número ingresado es divisible por 2 (es decir, si es par)
if numero % 2 == 0:
    # Si el residuo de la división entre 2 es 0, el número es par
    print("El número es par")
else:
    # Si el residuo no es 0, entonces el número es impar
    print("El número es impar")
